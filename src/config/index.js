const SNOOZE_LIMIT_COUNT = 3; // an alarm can only be snoozed 3 times at max.
const SNOOZE_INTERVAL = 5; // alarm is snoozed by a 5 mins snooze interval

module.exports = {
    SNOOZE_LIMIT_COUNT,
    SNOOZE_INTERVAL,

}